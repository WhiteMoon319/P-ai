# 未发布

- 功能（appearance）：全局 UI 界面尺寸改为 75%–150% 连续调节，同步调整文字语义 token 与 DaisyUI 控件尺寸；移除 WebView 整页缩放及自定义主题中的松紧度设置。
- 优化（appearance）：应用内文字字号统一使用全局语义 token，Markdown 保留标题、表格与代码的层级。
- 优化（appearance）：UI 界面尺寸滑块增加 75% / 100% / 125% / 150% 快捷档位。
- 修复（markdown）：支持尖括号包裹的 URL、file URI 与 Windows 文件自动链接。
