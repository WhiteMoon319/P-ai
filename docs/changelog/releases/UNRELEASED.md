# 未发布

## 功能

- 会话 Shell 工作目录新增“在此目录 / 在隔离工作树”工作位置选择；隔离模式会向模型注入 `.pai/.worktree/` 工作约束，并在只读权限下提示至少启用审批权限。

## 修复

- VS Code IDE 桥不再捕获编辑器可视区域；同一文件有选区时只同步带行号的选区，没有选区时只同步不带行号的当前文件引用。

- 永久关闭输入面板的侧边文件标签入口，并忽略旧本地偏好与跨窗口开启事件；保留实现代码和 IDE 桥文件标签能力。

- 修复远程应答委托后端链路误用前端展示消息投影，避免工具历史展示文本回流到模型上下文或持久化流程。

## 开发体验

- 开发模式下可从消息气泡右键菜单查看对应的原始 `ChatMessage` JSON，便于排查投影前后的消息结构与展示问题。
