# 未发布

- 修复（new-conversation）：为新建会话弹窗补充“会话中途不可切换部门/人格”提示，并统一接入多语言词条。
- 修复（chat-toolbar-mention-popup）：将聊天工具栏的人格按钮改为无边框 `@`，并让弹出列表复用输入框 `@` 候选窗的自适应定位与滚动样式，避免超出屏幕。
- 修复（chat-elastic-space）：当会话中找不到用户消息时，弹性空间回退锚定摘要或最后一条可见消息，避免工具条顶到窗口顶部。
