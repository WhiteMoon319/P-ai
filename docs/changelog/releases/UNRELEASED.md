# 未发布

## 修复

- 修复聊天输入框的当前模型名称偶尔不显示思维档位的问题。
- 修复未安装 rg 时仍提示优先使用 rg 的问题，减少不可用命令带来的执行失败。

## 优化

- 优化模型选择器的层级展示：供应商层始终保留，打开时自动展开当前供应商和当前模型，其他供应商默认收起。
- 单一思维档位的模型直接在模型行显示档位，减少不必要的折叠菜单。
- 支持在模型选择菜单中右键收起所有供应商。