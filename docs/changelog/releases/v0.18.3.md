# 发布：v0.18.3

## 功能

- 会话列表分组新增旧会话渐进加载策略：默认隐藏 7 天未使用的旧会话、每组至少保留 5 条，并支持“加载更多”逐步展开。

## 维护

## 修复

- 修复 `Shift + 滚轮` 切换会话失效的问题，并在切换到目标会话后自动展开所属的非“最近会话”分组，减少列表定位断裂感。
- 修复聊天窗口“上一条/下一条用户消息”跳转按钮的位置与显示样式问题，统一入口层级并补充轻量显隐动画。

## 重构

## 优化

- 会话列表分组在 tab 隐藏超过 30 秒后会自动收起通过“加载更多”展开的旧会话，短暂切换 tab 时则保留当前浏览上下文。
