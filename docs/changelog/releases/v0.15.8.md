# v0.15.8

## 功能

- 支持会话分组排序与拖拽调整
- 设置页和导航侧栏使用浮动滚动条，hover 时显示
- 输入框焦点下按 Esc 打断 LLM 回复

## 修复

- 修复 chat/archives 窗口隐藏再唤出时尺寸重置为默认值的问题；改用 inner_size 持久化窗口布局，提升多事件源调试可观测性
- 窗口失焦/最小化时保留前台流式状态，避免无谓的流式重绑
- 切换未归档对话时检查活跃 delta 通道，防止重复绑流
