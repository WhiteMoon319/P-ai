# 变更日志

> 此文件由 `pnpm changelog:build` 自动生成，展示最近版本的完整说明。

## 发布：v0.15.7

## 功能

- **代码审查人格选择**：审查对话框现在支持精确选择部门下的特定人格作为审查者，而非自动选中部门首个人格；跨会话、归档和侧边栏全链路透传 agentId。

## 修复

- **Markdown 行内样式**：修复粗体内容包含行内代码时 `**` 无法闭合、原样显示的问题。
- **自动更新弹窗**：将新版本提醒从窗口内横幅卡片改回弹窗，并保留跳过版本与取消下载操作。
- **聊天输入栏**：发送按钮改用折角上箭头图标，并统一计划模式胶囊与发送按钮的间距和高度。

## 发布：v0.15.6

## 修复

- **恢复消息去重**：恢复会话时去重已落库的 assistant 消息和前端草稿，避免重新打开窗口后出现两条相同回复。
- **新建会话提示**：新建会话弹窗补充“会话中途不可切换部门/人格”提示，并统一接入多语言词条。
- **工具栏人格弹窗**：聊天工具栏的人格按钮改为无边框 `@`，弹出列表复用输入框 `@` 候选窗的自适应定位与滚动样式，避免超出屏幕。
- **聊天弹性空间**：当会话中找不到用户消息时，弹性空间回退锚定摘要或最后一条可见消息，避免工具条顶到窗口顶部。

## 发布：v0.15.5

## 修复

- **goal 续跑完成态收口**：移除 `round_completed` 对前端 activation 门禁的错误依赖，goal 自动续跑在消息正式完成后会正确结束忙碌态，不再出现已经出正式消息却仍显示“正在输入”的情况。
- **goal 流式旧包拒收**：前端在收到完成信号后会拒收同轮次残留的旧流式增量，避免已完成消息再次把草稿气泡、思考中状态或停止按钮拉回活动态。
- **goal 草稿定位与测高**：助理草稿统一追加到消息尾部，正式消息完成时不再复用流式草稿的渲染身份，减少系统消息插入后出现的气泡错位、跳动和虚拟列表测高残留。
- **窗口恢复旧 busy 残留**：聊天窗口最小化、还原或重新激活时不再盲信前端残留的忙碌态，而是以后端运行态快照为准，避免流式已完成但界面仍卡在旧 busy 状态。
- **目标续跑投递语义**：目标续跑消息改为助理角色并延后一秒投递，避免把 persona 名为“系统”的消息误当成 LLM system prompt，同时减少续跑消息在用户刚看清上一条回复前抢占视线。
- **归档重建口径**：rewind 与归档链路的 `body_text_length` 重建改为使用统一口径，减少会话归档统计和后续重建不一致的问题。

## 发布：v0.15.4

## 新增

- **助理空间工作区统一**：主助理会话统一落到助理空间工作区，减少会话切换时的工作区割裂，后续工具与记忆链路也更容易复用同一上下文。
- **更新提醒卡片**：应用内更新入口改为提醒卡片展示，支持跳过指定版本，并在可中断阶段提供取消更新操作。

## 优化

- **更新检查对话**：手动检查更新后会直接展示当前版本、最新版本、运行时类型和发布说明，下载入口与状态提示更完整。

## 修复

- **ready 快照重复自修复**：消息存储 ready 校验改为按持久化视角比较索引，避免把临时重建索引和磁盘索引误判为不一致，导致每次启动都重建会话快照。
- **更新取消状态**：更新中的取消按钮只在真正可取消阶段展示，取消中、完成、失败和重启就绪后的状态收口更一致，避免前端错误暴露不可用操作。

## 发布：v0.15.3

## 新增

- **会话快速请求**：统一记录远程 IM、标题生成、任务优化和工具评估等会话级轻量请求，并在右侧栏新增「杂务」页用于查看请求、回复和错误记录。

## 优化

- **杂务任务名称**：远程联系人会话里的杂务记录按具体轻量任务显示“回复判断”“忙碌打断判断”，新记录不再笼统显示为“远程 IM”。

## 修复

- **远程 IM 工具轮次回复**：远程 IM 联系人调度在工具调用前已生成的助理文本，会在工具结果持久化后自动发送给本轮绑定联系人；最终收尾自动发送改为只按最终回复尾段判定，避免任务期间说过的话被漏发或重复外发。
- **始终回复秘书判断**：始终回复联系人统一进入秘书决策方法内部短路通过，避免普通入场或忙碌引导在调用方按策略分叉；该短路不请求快速模型、不记录杂务、不输出秘书判断日志。

## 其他

- **VS Code 侧边栏扩展**：同步扩展版本号并完成 `pai-test.vsix` 打包验证。

## 发布：v0.15.2

## 调整

- **快捷键设置**：快捷键页将录音键、录音测试、后台语音截图关键词与截图范围合并为“语音”卡片，聊天设置页不再重复展示后台语音截图设置。
- **用量管理**：移除聊天设置页的多媒体解析缓存卡片，改由用量页统一展示并清理。
- **会话列表**：新增置顶下方的“最近会话”分组，默认仅展开最近会话，其它分组默认折叠；最近会话额外纳入一小时内和所有未读会话。

## 修复

- **工具调用**：为 `get_session`、`recall`、`reload` 内置工具补齐空 `required` schema，避免 DeepSeek 等严格校验供应商拒绝工具调用请求。
- **后台唤醒**：后台唤醒开关改为即时写入配置、刷新录音热键探针并广播最新配置，避免仅切换设置页草稿导致运行时不生效。
- **会话接管**：当前会话绑定部门或人格失效时显示接管人选择/删除入口，避免聊天窗口卡死。
- **弹层输入**：原生选区清理收敛为弹层打开瞬间触发，避免全局清理持续打断委托等文本输入框。

## 发布：v0.15.1

## 优化

- **聊天右侧栏**：委托、任务和工具评估项统一改为整项点击打开，补齐和任务一致的悬停高亮，并复用现有会话列表时间格式显示开始/完成时间。

## 修复

- **远程联系人**：新联系人会话现在会像普通会话一样注入初始摘要；当消息仓库还没有可续写 block 时，追加消息会自动兜底，避免新群消息进入后所有远程联系人会话一起报错。
- **前台同步**：运行态事件命中新远程会话时，前端会补拉远程会话列表，避免前端收到运行状态后因为本地列表缺项而静默不刷新。

## 其他

- **VS Code 侧边栏扩展**：随本次前端改动同步重打安装包，便于发布前验证。

## 发布：v0.15.0

## 新增

- **聊天窗口**：Markdown 渲染支持注脚、README 图片徽章和本地相对图片路径，阅读文档、README 与模型输出时更接近原始排版。
- **聊天窗口**：提示词预览支持切换本地会话与远程联系人会话类型，方便在发送前确认不同场景下的实际上下文。
- **用量统计**：新增按供应商和模型细分的用量统计，并增强旧数据兼容，账单与模型消耗更容易追踪。

## 优化

- **聊天窗口**：流式 Markdown 改为增量解析，只重算尾部未完成内容，长回复、代码块、表格和工具引用的显示更顺滑。
- **聊天窗口**：引用块、代码块、表格、文档标题和深色主题细节统一打磨，阅读层级更清楚。
- **聊天窗口**：历史消息加载触发、图片/表情显示、图片预览关闭按钮和右键选区菜单体验进一步收口。
- **远程联系人**：统一远程联系人消息读取、身份提示词与模型预览，OneBot 消息段渲染和图片显示更稳定。
- **任务侧栏**：聊天侧任务创建卡、任务列表与工具评估侧栏复用同一套交互，任务管理入口更一致。
- **文件阅读器**：重构文档阅读、渲染和滚动交互，补齐独立文件窗口的多语言注入。

## 修复

- **启动稳定性**：修复首次启动或删除最后会话后 ready 消息仓库恢复链路中的会话锁重入死锁。
- **聊天窗口**：助理最终正文与思考文本会原样持久化，避免空白、缩进或空正文在提交阶段被改写。
- **聊天窗口**：修复流式纯换行内容被丢弃、自定义语言代码块底色露缝、超长错误提示挤走关闭按钮等问题。
- **工具评估**：补丁预览现在只显示实际改动内容，并复用真实命中行范围，行号和评估意见展示更准确。
- **快速设置与选择器**：补齐快速设置步骤文案，统一部门人格选择器并修正头像接线。

## 其他

- **VS Code 侧边栏扩展**：版本同步更新至 0.15.0，并随本次前端改动重新打包验证安装包。
- **依赖维护**：DaisyUI 更新至 5.6.0。

## 发布：v0.14.0

## 新增

- **聊天窗口**：增强任务 CRON 编辑体验

## 修复

- **聊天窗口**：修复超长错误提示挤走关闭按钮

## 其他

- **VS Code 侧边栏扩展**：版本同步更新至 0.14.0

## 发布：v0.13.9

## 修复

- **远程联系人**：远程联系人独立会话不再应用会话首选模型覆盖，回复始终跟随绑定部门模型；同步联系人会话绑定时会清理残留首选模型，避免历史会话继续误用旧模型

## 其他

- **VS Code 侧边栏扩展**：版本同步更新至 0.13.9

## 发布：v0.13.8

## 修复

- **配置编辑**：模型卡片的图片/视频能力开关改为使用模型数据库返回的能力字段；数据库未命中时保留人工配置入口，避免按模型名推断导致多模态开关消失
- **多模态模型选择**：多模态分析模型候选改为尊重已保存的 `enableImage` / `enableAudio` / `enableVideo` 能力，不再依赖模型名推断

## 其他

- **VS Code 侧边栏扩展**：版本同步更新至 0.13.8

## 发布：v0.13.7

## 新增

- **对话输入**：@ 提及智能体时支持按 @ 后输入的内容过滤候选列表，匹配智能体名称或所属部门名称
- **新建会话**：新增 AgentSelector 组件，按部门分组以卡片形式展示人格头像，底部支持模型切换；标题栏与侧边栏新建会话弹窗均已切换为该组件
- **IDE 上下文引用**：新增无行号文件级胶囊，引用整文件时展示更清晰
- **自动记忆注入**：场景提示不再附带 reasoning 文本，减少无关噪音

## 修复

- **聊天消息持久化**：助理消息落盘时不再把 `[toolcall:*]` 展示标记写进正文，避免历史消息出现假的工具图标或不可点击的工具占位
- **撤回**：撤回助理消息时不再错误地把上一条用户消息也撤回；后端放宽撤回目标角色校验，前端「撤回」与「重新生成」拆分处理，助理消息撤回不再映射到上一条用户消息
- **聊天窗口**：从标题栏新建会话时，默认工作目录优先继承当前会话的主工作目录，避免默认选中助理空间
- **聊天窗口**：修正工作目录路径显示与间距，调整打断按钮启用时机，优化发送后自动滚底并规避虚拟列表二次下推
- **工具调用展示**：修正工具折叠条摘要显示，避免工具调用信息误导
- **侧边栏**：对齐发送草稿与滚动行为，避免发送后视图状态不一致
- **代理与工作目录**：修正子代理继承工作目录，并拆分 shell 工作目录与助理空间提示，减少目录语义混淆
- **配置编辑**：新建人格和部门后立即持久化，避免新增后状态未及时落盘

## 其他

- **VS Code 侧边栏扩展**：版本同步更新至 0.13.7
