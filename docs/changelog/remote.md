# 变更日志

> 此文件由 `pnpm changelog:build` 自动生成，展示最近版本的完整说明。

## 发布：v0.19.5

## 功能

## 维护

## 修复

- 修复（window-drag-restore-default-size）：标题栏拖动窗口时，若当前窗口已最大化或尺寸已接近屏幕 92%，先恢复到默认尺寸再继续拖动，避免贴边放大后直接拖动仍保持近全屏尺寸。
- 修复（web-settings-native-actions）：网页端接通用量概览 API，并隐藏设置页、聊天工作区与文件阅读器中的本机目录打开入口，避免浏览器环境暴露不可用的桌面能力。
- 修复（web-prompt-preview-bridge）：网页端请求体预览接通会话列表与系统提示词预览桥接，修复会话下拉框无内容、预览功能不可用的问题。
- 修复（markdown-list-marker-fidelity）：聊天 Markdown 渲染改为按列表项保留原文编号值，避免有序列表在夹杂无序子项或使用非常规编号时被自动改写为连续序号。
- 修复（chat-summary-state-misuse）：未归档会话、前台会话选择、聊天索引归档判定与请求体预览不再把 `summary` 当作生命周期依据；`summary` 仅保留为内容字段，归档/未归档判断统一只看正式归档字段。

## 重构

## 优化

## 发布：v0.19.3

## 功能

## 维护

## 修复

- 修复（chat-sidebar-attachment-downloads-and-missing-media）：侧边栏通过文件选择器添加附件时先统一入队并写入 `downloads/`，图片附件的预览与附件路径提示收敛为单一状态源，同时在历史媒体恢复阶段遇到缺失文件时跳过对应媒体并保持后续附件路径不串位。
- 修复（updater-cancel-during-download）：下载更新时按真实更新阶段判断取消按钮展示，检查/下载/校验/准备阶段重新显示“取消更新”，避免进度弹窗只剩确认入口。
- 修复（updater-auto-check-cooldown）：自动更新检查改为后端内存冷却控制，CD 内直接返回无更新，手动“检查更新”始终强制重新抓取远端，避免重复复读旧更新结果和旧本地版本号。
- 修复（chat-goal-refresh-after-round-complete）：聊天窗口在前台轮次完成后主动重新查询当前会话 goal，避免后端已把 goal 更新为 complete 后输入面板 GOAL 仍停留高亮。

## 重构

## 优化

## 发布：v0.19.2

## 修复

- 快速设置左上角入口改为“跳过快速设置”文字按钮，点击后更明确地进入设置页并隐藏快速设置窗口。

## 发布：v0.19.1

## 修复

- 修复（chat）：请求图片统一压缩为 WebP。
- 修复（chat）：修复附件注入与多模态图片发送。

## 发布：v0.19.0

## 功能

- 功能（config）：所有模型选择器统一展示模型思维等级，关闭思考时显示“不思考”，便于在快速设置、配置页和聊天相关入口中直接区分同模型的不同推理档位。
- 功能（chat）：启动加载遮罩补充阶段进度提示，启动过程更可感知。

## 维护

- 维护（updater）：自动更新检查统一收口到后端 8 小时节流，减少重复触发。

## 修复

- 修复（config）：软废弃删除供应商或模型后，会同步清理失效绑定、跳过已废弃供应商，并在历史用量读取模型名缺失时按旧 ID 自动回填；若已无法解析则写入 `unknown`，避免统计与配置入口被脏引用卡住。
- 修复（chat）：修正助理气泡左右留白不对称、回复期间对输入工具栏的误禁用，以及底部弹性区域未给 Todo 可见空间预留的问题。
- 修复（sidebar）：侧边栏流式消息重新收口到真实 assistant 消息 ID，避免消息归属错乱。

## 重构

## 优化

## 发布：v0.18.8

## 功能

## 维护

## 修复

- 修复（reasoning-disable-special-vendors）：配置页新增“思维强度 -> 无”，并为 DeepSeek、Kimi（Moonshot）、豆包这类特殊供应商在关闭思维链时只发送 `thinking.type=disabled`，不再错误附带 `reasoning_effort=none`，避免请求被 400 拒绝。

## 重构

## 优化

## 发布：v0.18.7

## 功能

## 维护

## 修复

- 修复（read-media-multimodal-routing）：`read_media` 不再按模型名或协议白名单拒绝已选中的多模态模型；只要配置页已允许绑定并开启图片/音视频能力，就允许执行媒体解析，避免 `doubao-seed-2.0-mini` 等模型出现“能选不能用”的致命错判。

## 重构

## 优化

## 发布：v0.18.6

## 功能

- Mermaid 图表新增复制按钮，便于直接复用生成内容。

## 维护

## 修复

- 修复助理激活状态下切换会话失败的问题。
- 修复 Mermaid 流式渲染过程中内容提前渲染导致显示不稳定的问题。
- 修复流式工具调用标记插入位置漂移的问题。

## 重构

## 优化

- 解除助理消息气泡宽度限制，并恢复 Mermaid 消息的宽气泡展示。
- 优化未思考状态展示，减少回复生成中的空状态干扰。

## 发布：v0.18.5

## 功能

- 聊天消息视图接入自研气泡组件，固定用户/助理消息样式并移除右键隐藏气泡入口。
- 恢复聊天消息右键菜单的气泡背景开关，支持按偏好切换助理消息背景。

## 维护

## 修复

- 修复 Markdown 围栏代码块前置 1-3 个空格时未被识别为代码块的问题。
- 修正聊天队列预览中用户来源未显示真实用户人格名的问题。
- 修复聊天气泡背景在仅有工具标记时显示空白最小气泡的问题。
- 修复后台录音热键按下后切回前台时松开无法停止录音的问题。
- 修复标题栏双击还原窗口时接近全屏尺寸不会回落到默认尺寸的问题。
- 修复创建会话分支时默认来源标题占用用户标题字段，导致后续自动标题无法生成的问题。

## 重构

## 优化

- 降低聊天 Markdown 代码块背景、标题栏和边框的不透明度，减少正文中的视觉重量。
- 收紧聊天队列预览样式，移除行背景和外层上下间距，改用轻分割线区分多条队列。
- 优化最近会话列表的分组展开行为，并在头像底部显示来源标识。

## 发布：v0.18.4

## 功能

## 维护

## 修复

- 修复便携版自动更新后旧 helper、staging、zip 和计划文件残留在更新临时目录的问题。
- 修复会话压缩期间 web 侧对话框阻塞会话切换，以及压缩完成后误刷新当前已切换会话的问题。
- 修复 web 模式回复期间无法继续发送队列消息，并接入队列预览、撤回和引导投送能力。
- 修复 web 模式消息右键菜单文字和图标未跟随主题色的问题。
- 修复 web 模式刷新后左右侧栏开关、模式和宽度布局丢失的问题，并统一 APP 与 web 的聊天布局持久化逻辑。
- 修复恢复会话时流式投影与已持久化流式消息通过分叉路径合并导致重复显示的问题。

## 重构

## 优化

## 发布：v0.18.3

## 功能

- 会话列表分组新增旧会话渐进加载策略：默认隐藏 7 天未使用的旧会话、每组至少保留 5 条，并支持“加载更多”逐步展开。

## 维护

## 修复

- 修复 `Shift + 滚轮` 切换会话失效的问题，并在切换到目标会话后自动展开所属的非“最近会话”分组，减少列表定位断裂感。
- 修复聊天窗口“上一条/下一条用户消息”跳转按钮的位置与显示样式问题，统一入口层级并补充轻量显隐动画。

## 重构

## 优化

- 会话列表分组在 tab 隐藏超过 30 秒后会自动收起通过“加载更多”展开的旧会话，短暂切换 tab 时则保留当前浏览上下文。

## 发布：v0.18.2

## 功能

- 聊天消息区新增“上一条/下一条用户消息”跳转按钮，并将回到底部入口调整为独立底部图标，长对话内移动更直接。

## 维护

- 清理 Rust 后端未使用代码，消除 `cargo check` 中长期出现的 dead code warning。

## 修复

- 修复远程访问登录态容易丢失的问题：登录后只保存 token，不再保存密码或密码摘要；后端支持多个有效 token 并滑动续期，避免侧边栏、设置页和不同浏览器入口互相挤掉登录态。
- 修复 web 模式下聊天输入区模型下拉菜单不跟随当前主题色的问题，Teleport 弹层现在会继承当前主题。
- 修复简单行内数学公式无法渲染的问题，减少 `$...$` 公式被误过滤的情况。
- 修复配置页侧栏滚动区域被标题栏顶开以及左右边距不一致的问题。
- 修复聊天窗口与侧边栏“从此消息开始创建会话分支”错误回退到上一条用户消息、导致分支漏掉当前消息的问题。
- 修复聊天窗口与侧边栏撤回消息时错误复用“回退到上一条用户消息”定位逻辑的问题，撤回与分支现在都直接作用于当前消息。

## 重构

- 拆分消息定位逻辑：仅“重新生成”继续使用“定位到对应用户消息”的专用路径，避免与撤回、会话分支共享错误语义。

## 优化
