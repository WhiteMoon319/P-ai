<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Shizuku Manager 授权权限（v11 前 API_V23；v11+ 走自实现权限，双声明兼容老版本） -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V11" />

    <!-- Shizuku 授权所需的交互跨用户权限（ShizukuProvider 保护） -->
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" />

    <application>
        <!-- Shizuku Provider：authorities 必须为 ${applicationId}.shizuku，由 AGP manifest merger 展开 -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            tools:ignore="ExportedProvider" />
    </application>
</manifest>
