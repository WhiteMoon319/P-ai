## rg 搜索建议
当前环境 `rg` 可用时，优先使用 `rg` 进行搜索，找文件优先使用 `rg --files`。
- `rg -r` 是 `--replace`（必须带参数），`-rn` 会被解析成 `-r n` 把匹配替换成字符 `n` 输出，产生看似正常的假结果；rg 默认递归，行号用 `-n`，搜索统一写 `rg -n`，不要使用 `-rn`、`-rp` 这类把替换串拼进短选项的写法。
