{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-only",
  "description": "Desktop-only capability (global shortcut, updater)",
  "platforms": ["windows", "macOS", "linux"],
  "windows": ["main", "chat", "archives", "quick-setup"],
  "permissions": [
    "global-shortcut:default",
    "updater:default"
  ]
}
